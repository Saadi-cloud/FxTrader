# Referral landing-page image fit fix

The left promotional image now uses `object-fit: contain` instead of `cover`, so no text or artwork is cropped. A blurred, darkened copy of the same artwork fills the background behind the full image for a polished desktop layout. Mobile displays the full image at its natural aspect ratio.

No database update is required.
